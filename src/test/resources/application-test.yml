spring:
  datasource:
    url: jdbc:tc:postgresql:15:///decisionfabric_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090
          jwk-set-uri: http://localhost:9090/.well-known/jwks.json

management:
  metrics:
    export:
      cloudwatch:
        enabled: false

ai:
  provider:
    config:
      ssm-parameter-path: /test/ai-provider
      poll-interval-seconds: 3600

logging:
  level:
    com.decisionfabric: DEBUG
